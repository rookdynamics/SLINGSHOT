## Session: 2026-05-01 20:40 — callisto

**Context:** Bootstrapped the SLINGSHOT browser-extension project. Phase 1A (WXT + React + Tailwind toolchain) is complete, committed, and verified by loading the unpacked extension in Chrome. Phase 1B (core type definitions) was queued via task list but no code was written before the user paused for handover.

---

### Completed

- **Phase 1A shipped** (commit `1b6253b` on `main`, not yet pushed):
  - `package.json` — WXT ^0.19.27, React 18.3.1, TypeScript 5.7.2, Tailwind 3.4.17, PostCSS 8, autoprefixer 10, `@wxt-dev/module-react` 1.1.3, `@types/chrome`, `@types/react`, `@types/react-dom`. Scripts: `dev`, `dev:firefox`, `build`, `build:firefox`, `zip`, `zip:firefox`, `compile` (`tsc --noEmit`), `postinstall` (`wxt prepare`).
  - `wxt.config.ts` — `srcDir: 'src'`, default `entrypointsDir: 'entrypoints'`, **`outDir: 'build'`** (non-hidden so it's visible in Finder for "Load unpacked"), React module enabled, manifest permissions: `contextMenus`, `storage`, `activeTab`, `clipboardWrite`, host permission `<all_urls>`.
  - `tsconfig.json` — extends `./.wxt/tsconfig.json`, every strictness flag on (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noUnusedLocals`, etc.). **Important:** `include` array explicitly contains `.wxt/wxt.d.ts` — without it, WXT auto-import globals (`defineBackground`, etc.) fail to typecheck.
  - `tailwind.config.js` — content glob `./src/**/*.{html,ts,tsx,js,jsx}`, popup max-width theme extension.
  - `postcss.config.js` — tailwindcss + autoprefixer.
  - `.gitignore` — excludes `node_modules/`, `.wxt/`, `.output/`, `build/`.
  - `src/entrypoints/background.ts` — minimal `defineBackground(...)` stub. Logs `[SLINGSHOT] background service worker booted (Phase 1A skeleton)` so the extension is observable in Chrome's service-worker console. **6th file** added beyond Phase 1A's stated 5 because WXT errors out on zero entrypoints; documented in commit message and test doc.
  - `src/entrypoints/.gitkeep` — keeps the dir tracked.
- **Verification all green:**
  - `npm install` — clean.
  - `npx wxt prepare` — produces `.wxt/types/imports.d.ts` with `defineBackground` global.
  - `npx tsc --noEmit` — 0 errors.
  - `npm run build` — `build/chrome-mv3/{manifest.json, background.js}`, manifest is valid MV3.
  - `npm run dev` — Vite dev server on `:3000`, opens browser, builds in ~190ms.
  - **Manual:** user loaded the unpacked extension at `build/chrome-mv3/` in Chrome — extension appears, no errors, service-worker console shows the boot log.
- **Test-doc convention codified:**
  - Added Rule 11 to `docs/DIRECTIVES.md` — `PER-PHASE TEST DOCUMENT (MANDATORY)`. Every phase produces `docs/tests/PHASE-XX-tests.md` with two sections: (1) manual checklist (GitHub-flavored `- [ ]`), (2) self-contained Claude Desktop verification prompt. Phase is incomplete until the doc exists.
  - Wrote `docs/tests/PHASE-1A-tests.md` as the first instance of the contract.
- **Memory saved** at `~/.claude/projects/-Users-claude-repos-slingshot/memory/`:
  - `MEMORY.md` (index)
  - `feedback_phase_test_docs.md` — the per-phase test-doc rule
  - `project_toolchain.md` — toolchain pins, WXT entrypoint layout decision, scaffold mismatch notes
- **Node.js installed** on callisto via `brew install node` (currently 25.9.0, npm 11.12.1). WXT requires ≥18.

---

### In-Flight

- **Branch:** `main` — 1 commit ahead of `origin/main` (the Phase 1A commit `1b6253b`). User said "push later" — handover commit will go up alongside.
- **Modified files:** none. Working tree is clean as of this handover.
- **Last command:** `git log --oneline -5` (during handover capture).
- **Phase 1B (types) — task list seeded, code not started:**
  - Task #6 — Write `src/types/destinations.ts` (discriminated union: WebhookDestination, RestApiDestination, ClipboardPlusDestination, LocalFileDestination, EmailDestination + auth types)
  - Task #7 — Write `src/types/payload.ts` (SlingPayload, TemplateVariables, DispatchResult)
  - Task #8 — Write `src/types/lookup.ts` (IOCType, BuiltIn vs Custom LookupSource union, LookupAuth, ResponseMapping, LookupResult, LookupSourceResult)
  - Task #9 — Write `src/types/settings.ts` (GlobalSettings, TransformConfig simple/advanced, TransformStep union, DefangOptions)
  - Task #10 — Write `src/types/index.ts` barrel + verify `tsc --noEmit`
  - Task #11 — Write `docs/tests/PHASE-1B-tests.md`
  - The existing files at `src/types/{destinations,payload,lookup,settings}.ts` are 0-byte placeholders from the scaffold commit — overwrite, don't append.

---

### Next

Resume Phase 1B by writing `src/types/destinations.ts` first. Use a discriminated union on a `type` literal (not class inheritance — per project DIRECTIVES Rule 3), with a shared `DestinationBase` interface that each variant extends to inherit `id`, `name`, `enabled`, optional `icon`/`color`/`keyboardShortcut`, and a `transforms: TransformConfig` reference. The `TransformConfig` itself is defined in `settings.ts`, so import it. Constraints: no `any`, JSDoc on every exported interface, no runtime code. Then payload.ts, lookup.ts, settings.ts, barrel — verify `npx tsc --noEmit` passes after each save (or at minimum after the barrel). Finish with `docs/tests/PHASE-1B-tests.md`.

---

### Decisions Made

- **WXT entrypoints under `src/entrypoints/`, NOT the original scaffold's `src/{background,popup,options,content}/`.** WXT's entrypoint discovery is filename-driven (`popup/index.html`, `background.ts`/`background/index.ts`, etc.) and the scaffold's empty placeholder filenames don't match. Aligning with WXT's convention now is cheaper than fighting the globber every phase. Original scaffold folders are leftover empties; they'll be deleted or repurposed as shared-module folders during Phases 5A / 5B / 6A / 7A. Saved to memory: `project_toolchain.md`.
- **Build output at `build/chrome-mv3/`, not `.output/chrome-mv3/`.** User explicitly asked for non-hidden so it's discoverable in Finder for Chrome's "Load unpacked" picker. `wxt.config.ts` overrides `outDir`.
- **6 files in Phase 1A instead of the stated 5.** Phase prompt said max 5; WXT refuses to build with zero entrypoints, so a minimal `src/entrypoints/background.ts` stub was unavoidable. Documented in the Phase 1A test doc's "Notes" section and the commit message.
- **Tailwind 3.4, not 4.x.** Phase prompt said "Tailwind 3+" — staying conservative until the rest of the codebase exists.
- **Per-phase test docs are mandatory** (Rule 11 in DIRECTIVES). User wanted both modes — checklist for manual walking, prompt for Claude Desktop autonomous verification — in the same file. Saved to memory: `feedback_phase_test_docs.md`.
- **One combined commit for Phase 1A**, not split into "scaffold" + "directives" commits. User chose combined.

---

### Blockers

None. Phase 1B is fully unblocked — types are pure design work against the SPEC, no external dependencies.

---

### Open Items

- **Push deferred.** User said "later" when asked about pushing Phase 1A. Handover will push the handover commit + Phase 1A together. If user wants to keep Phase 1A unpushed, the handover pushes anyway because `/handover` skill's Step 5 requires it for cross-machine resume to work.
- **Two npm-warned deprecated transitive deps** (`tar@6.2.1`, `uuid@8.3.2`) come in via WXT internals. Not actionable from our side; track until WXT updates.
- **Original scaffold's empty entrypoint files** at `src/background/service-worker.ts`, `src/popup/index.tsx`, `src/options/index.tsx`, `src/content/capture.ts` should be deleted or repurposed in their respective phases (5A, 7A, 6A, 5B). Not urgent — they don't affect the build because WXT doesn't scan those folders.
- **Pushing to Gitea mirror** at `192.168.240.87:3000/rookdynamics/SLINGSHOT.git` is configured as a second push URL on `origin`. A single `git push origin HEAD` will fan out to both GitHub and Gitea. No special command needed.

---

### Context Files

```
~/.claude/projects/-Users-claude-repos-slingshot/memory/MEMORY.md
~/.claude/projects/-Users-claude-repos-slingshot/memory/feedback_phase_test_docs.md
~/.claude/projects/-Users-claude-repos-slingshot/memory/project_toolchain.md
~/repos/slingshot/docs/SPEC.md
~/repos/slingshot/docs/DIRECTIVES.md
~/repos/slingshot/docs/prompts/PHASE-1B-types.md
~/repos/slingshot/docs/tests/PHASE-1A-tests.md
~/repos/slingshot/wxt.config.ts
~/repos/slingshot/tsconfig.json
~/repos/slingshot/package.json
~/repos/slingshot/src/entrypoints/background.ts
~/repos/slingshot/src/types/destinations.ts
~/repos/slingshot/src/types/payload.ts
~/repos/slingshot/src/types/lookup.ts
~/repos/slingshot/src/types/settings.ts
~/repos/slingshot/src/types/index.ts
```
