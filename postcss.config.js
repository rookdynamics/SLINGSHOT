// SLINGSHOT — PostCSS configuration
// Standard pipeline: Tailwind first (expands utility classes), Autoprefixer
// second (adds vendor prefixes for the Chrome/Firefox MV3 target).

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
